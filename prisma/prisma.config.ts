import { defineConfig } from "@prisma/internals";

export default defineConfig({
  // For Prisma 7, connection URLs are passed to PrismaClient constructor
  // See: https://pris.ly/d/prisma7-client-config
});
